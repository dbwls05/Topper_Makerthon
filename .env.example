# Supabase 대시보드 (Project Settings → API)에서 값을 복사해 .env에 붙여넣으세요.
# 두 값이 채워지면 실제 회원가입이 저장되고, 비어 있으면 데모 모드로 동작합니다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 아래 두 값은 정부24 동기화 스크립트(npm run sync:gov24)에서만 쓴다.
# 브라우저에 노출되면 안 되므로 절대 VITE_ 를 붙이지 말 것.
DATA_GO_KR_KEY=
SUPABASE_SERVICE_ROLE_KEY=
